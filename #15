# 14. Mínimo común múltiplo (LCM)
a, b = 12, 15  # Ejemplo
mcd = a
while b != 0:
    mcd, b = b, mcd % b
mcm = abs(a * b) // mcd
print("Minimo comun multiplo:", mcm)

# 15. Área de un triángulo
base, altura = 10, 5  # Ejemplo
area = (base * altura) / 2
print("Área del triángulo:", area)